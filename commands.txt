What should I connect to?
    run commands? DONE
    rdp? Probably not
    take screenshot of screen and send it to email? or client?
    RAT?
    key logging, screen and camera capture, file access, code execution, registry management, password sniffing etc.

Commands:
    need to be logged in

    Running terminal commands:
        cmd **Command**
        cmd ls

